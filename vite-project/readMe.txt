This website runs used Vite and NODE JS to run a local server necessary to gain reference to my own models.

node js download: https://nodejs.org/en/download

vite installed through node using "npm install vite@latest" at VS code terminal

The project uses three.js installed using "npm install three"
imported into js using "import * THREE from "three". This installs the packages necessary to create a 3D scene.